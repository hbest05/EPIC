# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0          # async PostgreSQL driver
alembic==1.13.1

# Auth / security
# python-jose replaced by PyJWT — PYSEC-2024-232 (algorithm confusion) and
# PYSEC-2024-233 (DoS on decode) make python-jose 3.3.0 unsafe (COMP-01).
PyJWT[cryptography]>=2.8.0         # JWT encode/decode
cryptography>=42.0.0               # Required by PyJWT RS/EC algorithms and auth.py directly
argon2-cffi==23.1.0               # Argon2id password hashing
passlib[argon2]==1.7.4
slowapi==0.1.9                     # Request rate limiting (5/min auth, 60/min messaging)

# Redis (blockchain write queue)
redis[hiredis]==5.0.4
# aioredis==2.0.1

# Validation / serialisation
pydantic==2.7.1
pydantic-settings==2.2.1

# Blockchain
web3>=6.0.0              # AsyncWeb3 + AsyncHTTPProvider for on-chain calls
python-dotenv>=1.0.0     # .env loading (also used transitively by pydantic-settings)

# Dev / testing
pytest==8.2.0
httpx==0.27.0            # async test client for FastAPI
pytest-asyncio==0.23.6
